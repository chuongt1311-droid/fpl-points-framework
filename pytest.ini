[pytest]
markers =
    network: real network calls to an external source (e.g. understat.com).
             Not run by default in CI/weekly.yml's normal suite — see
             tests/test_understat_adapter.py's module docstring. Run
             explicitly with: pytest tests/ -v -m network
addopts = -m "not network"
